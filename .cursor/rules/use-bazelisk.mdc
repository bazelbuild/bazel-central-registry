---
description: Use Bazelisk for Bazel commands in this repository
alwaysApply: true
---

# Bazel Commands

Use `bazelisk` instead of `bazel` for all Bazel commands in this repository.

For example:

```bash
bazelisk run //tools:bcr_validation -- --check coroutines@3.3.2
```
