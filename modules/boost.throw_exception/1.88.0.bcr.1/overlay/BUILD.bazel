load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "boost.throw_exception",
    hdrs = glob([
        "include/**/*.hpp",
    ]),
    includes = ["include"],
    visibility = ["//visibility:public"],
    features = [
        "parse_headers",
    ],    deps = [
        "@boost.assert",
        "@boost.config",
    ],
)