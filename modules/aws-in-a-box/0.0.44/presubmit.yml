bcr_test_module:
  module_path: "."
  matrix:
    platform: ["debian10", "macos", "ubuntu2004"]
  tasks:
    run_tests:
      name: "Run tests"
      platform: ${{ platform }}
      test_targets:
        - "//..."
