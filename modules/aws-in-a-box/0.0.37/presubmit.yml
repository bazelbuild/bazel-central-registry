bcr_test_module:
  matrix:
    platform: ["debian10", "macos", "ubuntu2004", "windows"]
  tasks:
    run_tests:
      name: "Run tests"
      platform: ${{ platform }}
      test_targets:
        - "//..."
