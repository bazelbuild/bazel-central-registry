module(
    name = "boost.lockfree",
    version = "1.87.0",
    bazel_compatibility = [">=7.2.1"],  # need support for "overlay" directory
    compatibility_level = 108700,
)

bazel_dep(name = "rules_cc", version = "0.1.3")

bazel_dep(name = "boost.interprocess", version = "1.87.0", dev_dependency = True)
bazel_dep(name = "boost.test", version = "1.87.0.bcr.1", dev_dependency = True)
bazel_dep(name = "boost.thread", version = "1.87.0", dev_dependency = True)
