matrix:
  platform:
  - debian11
  - ubuntu2404
  - macos
  - macos_arm64
  bazel:
  - rolling
  - 8.x
  - 7.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@libbacktrace//:libbacktrace'
