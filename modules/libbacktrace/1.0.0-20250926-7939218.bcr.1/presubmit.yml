matrix:
  platform:
  - debian11
  - ubuntu2404
  - macos
  - macos_arm64
  bazel:
  - rolling
  - 8.x
  - 7.x
tasks:
  test_api:
    name: Test libbacktrace API
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    test_targets:
      - '@libbacktrace//test:api_test'
