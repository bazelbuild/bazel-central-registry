matrix:
  platform:
  - centos7
  - debian10
  - ubuntu2004
  - macos
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    build_targets:
    - '@rules_cc_hdrs_map//rules:*'

bcr_test_module:
  module_path: "examples"
  matrix:
    platform: ["debian10", "macos", "ubuntu2004"]
  tasks:
    run_tests:
      name: "Run examples module"
      platform: ${{ platform }}
      test_targets:
        - "//..."
