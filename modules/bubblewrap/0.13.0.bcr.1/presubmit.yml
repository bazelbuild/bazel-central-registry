matrix:
  platform:
    - debian11
    - ubuntu2004
    - ubuntu2204
    - ubuntu2404
  bazel: [7.x, 8.x, 9.x]
tasks:
  build_bwrap:
    name: Build bwrap
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@bubblewrap//:bwrap"
bcr_test_module:
  module_path: tests/bcr
  matrix:
    platform:
      - debian11
      - ubuntu2004
      - ubuntu2204
      - ubuntu2404
    bazel: [7.x, 8.x, 9.x]
  tasks:
    run_test_module:
      name: Check bwrap --version
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - //...
