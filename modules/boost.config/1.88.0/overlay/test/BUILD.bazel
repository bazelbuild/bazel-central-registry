load("@rules_cc//cc:defs.bzl", "cc_library", "cc_test")

cc_library(
    name = "test",
    hdrs = ["test.hpp"],
)

cc_test(
    name = "has_detect_mismatch_fail",
    srcs = [
        "has_detect_mismatch_fail.cpp",
        "boost_has_detect_mismatch.ipp",
    ],
    deps = [
        ":test",
        "@boost.config",
    ],
)
