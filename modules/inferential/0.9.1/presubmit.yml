matrix:
  platform:
    - debian10
    - ubuntu2004
    - macos
    - macos_arm64
  bazel:
    - 7.x
tasks:
  verify_targets:
    name: Build inferential C++ library
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@inferential//cpp:inferential"
      - "@inferential//proto:inferential_cc_proto"
