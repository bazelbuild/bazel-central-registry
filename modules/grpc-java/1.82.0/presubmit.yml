matrix:
  platform: ["debian10", "macos", "ubuntu2004", "windows"]
  bazel: ["8.x", "9.x"]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@grpc-java//:java_grpc_library__external_repo_test'
