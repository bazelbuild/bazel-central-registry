
matrix:
  platform: ["debian10", "ubuntu2004", "macos", "macos_arm64", "windows"]
tasks:
  verify_device_build:
    name: "Build test"
    platform: ${{ platform }}
    bazel: "7.x"
    build_flags:
      - "--platforms=@pico-sdk//bazel/platform:rp2040"
    build_targets:
      - "//..."
