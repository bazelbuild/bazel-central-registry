matrix:
  platform:
    - ubuntu2604
  bazel:
    - 7.x
    - 8.x
    - 9.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@libsvtav1//:SvtAv1Enc'
      - '@libsvtav1//:SvtAv1EncApp'
