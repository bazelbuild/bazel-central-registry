matrix:
  platform:
    - linux
    - macos
    - windows
  bazel:
    - 7.x
    - 8.x
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@dlpack//:dlpack"
