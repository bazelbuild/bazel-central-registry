load(":boost_test_suite.bzl", "boost_test_suite")

# Benchmarks: upstream builds them but does not run them.
_BENCHMARKS = glob(["performance*.cpp"])

boost_test_suite(
    name = "upstream_tests",
    srcs = glob(["*.cpp"], exclude = _BENCHMARKS),
)
