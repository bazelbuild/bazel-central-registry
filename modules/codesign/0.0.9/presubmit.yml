bcr_test_module:
  module_path: "."
  matrix:
    platform: ["debian11", "macos", "windows"]
    bazel: ["8.x"]
  tasks:
    run_tests:
      name: "Build module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_flags:
        - "--experimental_remote_downloader="
      build_targets:
        - "//..."

