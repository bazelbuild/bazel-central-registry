bcr_test_module:
  module_path: tests/bcr
  matrix:
    platform:
      - debian10
      - ubuntu2004
      - macos
      - macos_arm64
      - windows
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      shell_commands:
        - bazel run //:run_buildozer
      build_targets:
        - //...
      test_targets:
        - //...
