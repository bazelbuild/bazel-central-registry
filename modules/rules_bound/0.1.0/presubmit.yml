matrix:
  platform:
    - debian11
    - ubuntu2204
    - macos_arm64
    - windows
  bazel:
    - 8.x
    - 9.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rules_bound//bound/..."
bcr_test_module:
  module_path: bcr_test
  matrix:
    platform:
      - debian11
      - ubuntu2204
      - macos_arm64
      - windows
    bazel:
      - 8.x
      - 9.x
  tasks:
    run_test_module:
      name: Run the test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - //...
