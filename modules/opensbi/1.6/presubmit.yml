bcr_test_module:
  module_path: "."
  matrix:
    platform: ["ubuntu2204"]
    bazel: ["8.x", "9.x"]
  tasks:
    build_firmware:
      name: "Build OpenSBI firmware"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "@opensbi//:fw_dynamic"
        - "@opensbi//:fw_jump"
