# libpcap

The BUILD file for libpcap is hand-written by converting the autoconf (`configure.ac`) files
to Bazel using [`rules_cc_autoconf`](https://periareon.github.io/rules_cc_autoconf/migration.html).
