bcr_test_module:
  module_path: 'Examples/bzlmod'
  matrix:
    platform: ['macos', 'ubuntu2004']
    bazel: ['7.x']
  tasks:
    run_tests:
      name: 'Perform smoke test with bzlmod example'
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - '//...'
