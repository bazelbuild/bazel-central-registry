bcr_test_module:
  module_path: Examples/bzlmod
  matrix:
    platform:
      - ubuntu2004
      - macos
    bazel:
      - 7.x
  tasks:
    run_test_module:
      name: Build test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //...
