package(default_visibility = ["//visibility:public"])

SRCS = glob(
    include = ["**/*"],
    exclude = [
        "BUILD",
        "BUILD.bazel",
        "WORKSPACE",
        "WORKSPACE.bazel",
        "REPO",
        "**/*.bazel",
    ],
)

exports_files(SRCS)

filegroup(
    name = "prjtrellis-db",
    srcs = SRCS,
)
