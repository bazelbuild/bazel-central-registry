load(":boost_test_suite.bzl", "boost_test_suite")

# Upstream `compile-fail` targets.
_COMPILE_FAIL = glob([
    "goto_error*.cpp",
    "operator_error*.cpp",
])

# Disabled upstream: the test hits a division by zero and ASan reports a stack
# use after return, see the comment in test/Jamfile.v2.
_DISABLED_UPSTREAM = glob(["return_derivative*.cpp"])

boost_test_suite(
    name = "upstream_tests",
    srcs = glob(["*.cpp"], exclude = _COMPILE_FAIL + _DISABLED_UPSTREAM),
)
