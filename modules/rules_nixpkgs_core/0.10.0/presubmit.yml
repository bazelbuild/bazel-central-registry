bcr_test_module:
  module_path: "testing"
  matrix:
    platform: ["ubuntu2204"]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      shell_commands:
        - curl -L https://nixos.org/nix/install | sh -s -- --no-daemon
      test_targets:
        - "//..."
