bcr_test_module:
  module_path: "tests"
  matrix:
    bazel: ["6.x", "7.x", "8.x"]
    # TODO(#97): add windows
    platform: ["debian11", "ubuntu2204", "windows", "macos", "macos_arm"]
  tasks:
    smoke_tests:
      name: "Run smoke tests"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
