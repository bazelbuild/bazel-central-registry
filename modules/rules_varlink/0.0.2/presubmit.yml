bcr_test_module:
  module_path: examples/ping
  matrix:
    platform: ["ubuntu2404"]
    bazel: ["9.x"]
  tasks:
    run_test_module:
      name: Run ping example
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
      test_targets:
        - "//..."
