load(":empty_rule.bzl", "empty_rule")

# And empty target. 
# This is just in place to make Bazel CI happy. 
# Otherwise CI complains about no targets to build for boost.pin_version
empty_rule(name = "empty")
