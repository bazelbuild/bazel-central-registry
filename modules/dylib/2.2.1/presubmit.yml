matrix:
  platform:
  - ubuntu2204
  - windows
  - macos
  bazel: [8.x]

tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@dylib//:dylib'
    test_targets:
    - '@dylib//:unit_tests'
