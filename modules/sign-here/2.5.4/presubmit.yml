bcr_test_module:
  module_path: "e2e/bzlmod"
  matrix:
    platform: ["macos_arm64"]
    bazel: ["8.x", "9.x"]
  tasks:
    run_tests:
      name: "Run e2e bzlmod test"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
