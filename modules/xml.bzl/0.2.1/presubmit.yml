bcr_test_module:
  module_path: ""
  matrix:
    platform: ["debian11", "macos", "ubuntu2204", "windows"]
    bazel: ["8.x", "7.x"]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
        - "-//tools:preset.update_test"
