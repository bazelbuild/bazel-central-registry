matrix:
  platform:
    - ubuntu2404
    - macos_arm64
  bazel:
    - 8.x
    - 9.x

tasks:
  verify_tools:
    name: Build MIG tools
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@apple_mig//:mig"
      - "@apple_mig//:migcom"
