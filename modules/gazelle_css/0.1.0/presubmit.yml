bcr_test_module:
  module_path: "bcr_test"
  matrix:
    bazel:
      - 9.x
      - 8.x
  tasks:
    verify:
      name: Build and test gazelle_css
      platform: ubuntu2204
      bazel: ${{ bazel }}
      build_targets:
        - "@gazelle_css//css/..."
