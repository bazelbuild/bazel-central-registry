load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "boost.charconv",
    srcs = glob(["src/**/*.cpp"]),
    hdrs = glob([
        "include/**/*.*pp",
        "src/**/*.*pp",
    ]),
    includes = ["include"],
    deps = [
        "@boost.assert",
        "@boost.config",
        "@boost.core",
    ],
)
