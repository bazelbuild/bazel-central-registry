bcr_test_module:
    module_path: 'e2e/bzlmod'
    matrix:
        bazel: ['7.7.1', '8.x', '9.x']
        platform: ['debian10', 'macos', 'ubuntu2004', 'windows']
    tasks:
        run_tests:
            name: 'Run test module'
            bazel: ${{ bazel }}
            platform: ${{ platform }}
            test_targets:
                - '//...'
