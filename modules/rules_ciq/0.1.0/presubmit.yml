matrix:
  platform:
  - macos
  - macos_arm64
  bazel:
  - 8.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@rules_ciq//tools:get_application_id'
    - '@rules_ciq//tools:measure_cft'
    - '@rules_ciq//tools:scale_image'
