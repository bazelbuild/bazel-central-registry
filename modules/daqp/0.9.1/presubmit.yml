matrix: &matrix
  platform:
    - debian11
    - ubuntu2204
    - ubuntu2404
    - macos
    - macos_arm64
    - windows
  bazel:
    - 7.x
    - 8.x
    - 9.x
    - rolling

tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@daqp//:daqp'
      - '@daqp//:daqp_eigen'

bcr_test_module:
  module_path: .
  matrix: *matrix
  tasks:
    run_test_module:
      name: Run daqp-eigen tests
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - '//:00_basic_qp'
        - '//:01_static_func'
        - '//:02_class'
        - '//:03_update'
        - '//:04_slack_sign'
        - '//:05_warmstart'
        - '//:06_general_hessian'
        - '//:07_symmetrize_hessian'
        - '//:08_unconstrained_check'
      test_targets:
        - '//:00_basic_qp'
        - '//:01_static_func'
        - '//:02_class'
        - '//:03_update'
        - '//:05_warmstart'
        - '//:06_general_hessian'
        - '//:07_symmetrize_hessian'
        - '//:08_unconstrained_check'
