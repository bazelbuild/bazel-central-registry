matrix:
  platform:
  - debian10
  - ubuntu2004
  - macos
  - macos_arm64
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    build_targets:
    - '@rules_multitool//...'
