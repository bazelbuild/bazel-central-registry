bcr_test_module:
  module_path: "examples/rust_to_rocq"
  matrix:
    platform: ["debian11", "macos", "ubuntu2204"]
    bazel: ["7.x", "8.x"]
  tasks:
    run_tests:
      name: "Build rust_to_rocq example"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//:all"
