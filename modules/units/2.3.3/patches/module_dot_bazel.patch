--- /dev/null
+++ MODULE.bazel
@@ -0,0 +1,13 @@
+"""
+A compile-time, header-only, dimensional analysis 
+library built on C++14 with no dependencies.
+"""
+
+module(
+    name = "units",
+    version = "2.3.3",
+    compatibility_level = 2,
+)
+
+bazel_dep(name = "rules_cc", version = "0.0.9")
+bazel_dep(name = "googletest", version = "1.14.0")
