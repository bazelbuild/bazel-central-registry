matrix:
  platform:
    - debian13
    - macos
    - macos_arm64
    - ubuntu2404
    - ubuntu2404_arm64
    - windows
  unix_platform:
    - debian13
    - macos
    - macos_arm64
    - ubuntu2404
    - ubuntu2404_arm64
  bazel:
    - rolling
    - 9.x
    - 8.x
    - 7.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@mediainfo//:mediainfo"
      - "@mediainfo//:analyze_test"
  run_tests:
    # The upstream analyze_test opens its Example.ogg runfile via a plain relative
    # argv path, which requires a materialized runfiles tree -- not guaranteed on
    # Windows CI, so it runs on the unix platforms and is build-verified on all.
    name: Run test targets
    platform: ${{ unix_platform }}
    bazel: ${{ bazel }}
    test_targets:
      - "@mediainfo//:analyze_test"
bcr_test_module:
  module_path: test
  matrix:
    platform:
      - debian13
      - macos
      - macos_arm64
      - ubuntu2404
      - ubuntu2404_arm64
      - windows
    bazel:
      - rolling
      - 9.x
      - 8.x
      - 7.x
  tasks:
    run_tests:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
