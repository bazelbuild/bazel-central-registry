matrix:
  platform:
    - debian10
    - macos
    - macos_arm64
    - ubuntu2004
    - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: [6.x, 7.x]
    build_targets:
      - '@tinyobjloader//:tinyobjloader'
