matrix:
  platform:
  - ubuntu2204
  - windows
  bazel: [7.x, 8.x]

tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@cpptrace//:cpptrace'
    test_targets:
    - '@cpptrace//test:unittest'
