load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "libunwind",
    hdrs = glob(["include/**/*.h"]),
    features = [
        "parse_headers",
    ],
    includes = ["include"],
    srcs = glob([
        "src/**/*.c",
        "src/**/*.h",
    ]),
    visibility = ["//visibility:public"],
)
