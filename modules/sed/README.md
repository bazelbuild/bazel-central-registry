# sed

This module is hand written and uses [`rules_cc_autoconf`](https://registry.bazel.build/modules/rules_cc_autoconf)
to detect platform-specific configuration at build time rather than relying on hard-coded defaults.
