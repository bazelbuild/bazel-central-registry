matrix:
  platform: ["macos", "ubuntu2004"]
  bazel: ["8.x", "9.x"]

tasks:
  verify_targets:
    name: "Verify build targets"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rules_robolectric//bazel:android-all"
