===================================================================
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -2,9 +2,9 @@
 
 module(
     name = "rules_robolectric",
     # Note: the publish-to-BCR app will patch this line to stamp the version being published.
-    version = "0.0.0",
+    version = "0.0.0-test.20260723",
     bazel_compatibility = [">=7.0.0"],
     compatibility_level = 1,
 )
 
