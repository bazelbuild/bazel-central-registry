diff --git a/BUILD.bazel b/BUILD.bazel
new file mode 100644
index 0000000..997cea9
--- /dev/null
+++ b/BUILD.bazel
@@ -0,0 +1,19 @@
+# Hey there! Before you start modifying this file, go check out https://github.com/dynacondev/boost.rules.tools for the tool you need!
+
+load("@boost.rules.tools//:tools.bzl", "boost_library")
+
+boost_library(
+    name = "json",
+    deps = [
+        "@boost.align//:align",
+        "@boost.assert//:assert",
+        "@boost.config//:config",
+        "@boost.container//:container",
+        "@boost.container_hash//:container_hash",
+        "@boost.core//:core",
+        "@boost.describe//:describe",
+        "@boost.mp11//:mp11",
+        "@boost.system//:system",
+        "@boost.throw_exception//:throw_exception",
+    ],
+)
diff --git a/MODULE.bazel b/MODULE.bazel
new file mode 100755
index 0000000..19beade
--- /dev/null
+++ b/MODULE.bazel
@@ -0,0 +1,24 @@
+"""
+Hey there! Most of this file is auto-generated by the tool at https://github.com/dynacondev/boost.rules.tools
+Go check it out to get started!
+Btw, The # Test Only section is manually edited!
+"""
+
+module(
+    name = "boost.json",
+    version = "1.83.0.bcr.1",
+    compatibility_level = 1,
+)
+
+bazel_dep(name = "boost.rules.tools", version = "1.0.0")
+bazel_dep(name = "platforms", version = "0.0.8")
+bazel_dep(name = "boost.align", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.assert", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.config", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.container", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.container_hash", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.core", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.describe", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.mp11", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.system", version = "1.83.0.bcr.1")
+bazel_dep(name = "boost.throw_exception", version = "1.83.0.bcr.1")
diff --git a/WORKSPACE.bazel b/WORKSPACE.bazel
new file mode 100644
index 0000000..be0754f
--- /dev/null
+++ b/WORKSPACE.bazel
@@ -0,0 +1 @@
+# SEE: MODULE.bazel
diff --git a/bazel_test/BUILD.bazel b/bazel_test/BUILD.bazel
new file mode 100644
index 0000000..8696913
--- /dev/null
+++ b/bazel_test/BUILD.bazel
@@ -0,0 +1,16 @@
+load("@boost.rules.tools//:tools.bzl", "boost_test_set")
+
+DEPS = [
+    "@boost.json//:json",
+]
+
+test_suite(
+    name = "tests",
+    tests = boost_test_set(
+        file_extensions = ".cc",
+        names = [
+            "json_test",
+        ],
+        deps = DEPS,
+    ),
+)
diff --git a/bazel_test/json_test.cc b/bazel_test/json_test.cc
new file mode 100644
index 0000000..2afc3bc
--- /dev/null
+++ b/bazel_test/json_test.cc
@@ -0,0 +1,6 @@
+#include <boost/json.hpp>
+
+int main() {
+  auto jv = boost::json::parse(R"({"hello": "world"})");
+  return jv.as_object().at("hello") != "world";
+}
