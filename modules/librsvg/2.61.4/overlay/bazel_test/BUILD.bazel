load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "svg",
    srcs = ["svg.c"],
    linkstatic = True,
    deps = [
        "@gdk-pixbuf",
        "@librsvg",
        "@librsvg//:pixbuf_loader",
    ],
)
