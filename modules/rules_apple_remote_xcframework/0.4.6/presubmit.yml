matrix:
  platform:
  - macos_arm64
  bazel:
  - 8.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@rules_apple_remote_xcframework//...'
bcr_test_module:
  module_path: examples/
  matrix:
    platform:
    - macos_arm64
    bazel:
    - 8.x
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
      - :test_lib
