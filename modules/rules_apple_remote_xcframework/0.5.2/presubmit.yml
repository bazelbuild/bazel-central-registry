matrix:
  platform:
  - macos_arm64
  bazel:
  - 8.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@rules_apple_remote_xcframework//remote_xcframework:extensions.bzl'
