===================================================================
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -1,3 +1,5 @@
-module(name = "platform_constraints")
+module(name = "platform_constraints",
+    version = "0.2.2",
+)
 
 bazel_dep(name = "package_metadata", version = "0.0.7", dev_dependency = True)
