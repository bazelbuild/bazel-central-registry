matrix:
  platform:
    - debian11
    - macos_arm64
    - ubuntu2204
    - ubuntu2404
    # - windows
  bazel: [8.x, 9.x, rolling]
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@zenoh-c//:zenoh-c'
bcr_test_module:
  module_path: tests
  matrix:
    platform:
      - debian11
      - macos_arm64
      - ubuntu2204
      - ubuntu2404
      # - windows
    bazel: [8.x, 9.x, rolling]
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //...
      test_targets:
        - //...
