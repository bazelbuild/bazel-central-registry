matrix:
  platform:
    - centos7
    - debian10
    - ubuntu2004
    - macos
    - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    build_targets:
      - '@iceoryx//:iceoryx_hoofs'
      - '@iceoryx//:iceoryx_posh'
      - '@iceoryx//:iceoryx_binding_c'
      - '@iceoryx//:roudi'
