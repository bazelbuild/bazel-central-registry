bcr_test_module:
  matrix:
    platform: [ubuntu2204,ubuntu2404]
    bazel: [8.x,9.x]
  module_path: "e2e/custom_py_deps"
  tasks:
    run_tests:
      name: "Run test module"
      bazel: ${{ bazel }}
      platform: ${{ platform }}
      test_targets:
        - "//..."