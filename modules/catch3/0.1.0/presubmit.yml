matrix:
  platform:
    - macos
    - macos_arm64
    - windows
  bazel: [9.x, rolling]
tasks:
  verify_targets:
    name: Verify Catch3 targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@catch3//:catch2'
      - '@catch3//:catch2_main'
      - '@catch3//src/Progmasoft:catch3'
