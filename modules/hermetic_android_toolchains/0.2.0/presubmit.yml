bcr_test_module:
  module_path: "."

  matrix:
    platform: ["macos", "ubuntu2204", "windows"]
    bazel: ["8.x", "9.x", "rolling"]

  tasks:
    verify_targets:
      name: "Verify build targets"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - '//...'
