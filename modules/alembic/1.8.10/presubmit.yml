matrix:
  bazel: [7.x, rolling]
  unix_platform: ["macos",  "macos_arm64", "ubuntu2004", "ubuntu2204"]

tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@alembic//:alembic"
