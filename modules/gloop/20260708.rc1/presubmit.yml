matrix:
  platform:
  - debian13
  - ubuntu2404
  bazel:
  - 9.x
  - 8.x
tasks:
  environment:
    CC: clang
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@gloop//gloop/tools:filewrapper_impl'
