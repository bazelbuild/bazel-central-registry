bcr_test_module:
  module_path: "internal/test/bcr"
  matrix:
    platform:
      - rockylinux8
      - debian10
      - ubuntu2004
      - macos
      - windows
    bazel:
      - 8.x
      - 9.x
  tasks:
    verify_targets:
      name: Verify build targets
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//:pylib"
