matrix:
  platform:
    - ubuntu2404
    - ubuntu2404_arm64
    - macos_arm64
  bazel:
    - 7.x
    - 8.x
    - 9.x
    - rolling
tasks:
  verify_targets:
    name: Verify public targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@spirv_tools//:spirv_tools'
      - '@spirv_tools//:spirv_tools_opt'
      - '@spirv_tools//:spirv_tools_link'
      - '@spirv_tools//:spirv_tools_lint'

bcr_test_module:
  module_path: tests
  matrix:
    platform:
      - ubuntu2404
      - ubuntu2404_arm64
      - macos_arm64
    bazel:
      - 7.x
      - 8.x
      - 9.x
      - rolling
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //...
      test_targets:
        - //...
