bcr_test_module:
  module_path: bcr_test
  matrix:
    bazel: [9.x, 8.x]
    platform: [ubuntu2204, macos_arm64]
  tasks:
    verify:
      name: Build and test the rules_web_e2e consumer
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "@rules_web_e2e//:distribution"
        - "@rules_web_e2e//runtime:package"
        - "//..."
      test_targets:
        - "//..."
