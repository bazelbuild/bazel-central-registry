matrix:
  platform:
  - debian11
  - ubuntu2204
  - macos
  - macos_arm64
  bazel:
  - 9.x
  - 8.x
  - 7.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@fftw//:fftw_double'
    - '@fftw//:fftw_float'
    - '@fftw//:fftw'
    test_targets:
    - '@fftw//:bench_double'
    - '@fftw//:bench_float'
