matrix:
  platform: ["debian13", "ubuntu2404"]
  bazel: ["7.x", "8.x", "9.x"]
tasks:
  verify_targets:
    name: "Build and test"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    test_targets:
      - "//..."
