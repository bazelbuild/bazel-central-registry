bcr_test_module:
  module_path: "e2e"
  matrix:
    bazel: ["8.x", "9.*"]
    # TODO(#9): add windows to this list
    platform: ["debian10", "macos", "ubuntu2004"]
  tasks:
    run_tests:
      name: "Run test module"
      bazel: ${{ bazel }}
      platform: ${{ platform }}
      test_targets:
        - "//..."
