---
bcr_test_module:
  module_path: "example/integration_tests/bzlmod"
  matrix:
    platform:
      - debian11
      - ubuntu2404
      - macos
      - macos_arm64
      - windows
    bazel: [7.*, 8.*, 9.*]
  tasks:
    build_bzlmod_test:
      name: "Build test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//sample"
