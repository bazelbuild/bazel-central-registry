load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "boost.detail",
    hdrs = glob([
        "include/**/*.hpp",
        "include/**/*.h",
        "include/**/*.ipp",
    ]),
    includes = ["include"],
    deps = [
        "@boost.config",
        "@boost.core",
        "@boost.preprocessor",
        "@boost.static_assert",
        "@boost.type_traits",
    ],
    features = [
        "parse_headers",
    ],
)
