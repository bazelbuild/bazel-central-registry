tasks:
  verify_build_targets:
    name: Verify Build targets on macOS
    platform: macos
    build_flags:
      - --macos_minimum_os=13.0
    build_targets:
      # Temporarily just build the `swift_proto_library` target
      - "@swift_bep_parser//BuildEventProtocol:build_event_stream_proto_swift_internal"
