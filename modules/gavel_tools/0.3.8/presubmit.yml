matrix:
  platform: ["ubuntu2004"]
  bazel: ["7.x", "8.x"]
tasks:
  verify:
    name: "Build and test gavel_tools"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@gavel_tools//..."
    test_targets:
      - "@gavel_tools//..."
