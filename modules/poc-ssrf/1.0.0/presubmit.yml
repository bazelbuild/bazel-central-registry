matrix:
  platform:
  - ubuntu2204
  bazel:
  - "7.x"
tasks:
  verify_targets:
    name: Verify module loads
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@poc-ssrf//:all'
