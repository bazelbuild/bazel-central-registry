bcr_test_module:
  module_path: '.'
  matrix:
    platform: ["debian10", "macos", "ubuntu2004", "windows"]
    bazel: ["7.x"]
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - "@copybara//java/com/google/copybara:copybara"
