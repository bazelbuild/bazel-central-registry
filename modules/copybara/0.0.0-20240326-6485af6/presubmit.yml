matrix:
  platform: ["debian10", "macos", "ubuntu2004", "windows"]
  bazel: ["7.x"]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    module_path: ""
    build_targets:
    - "@copybara//java/com/google/copybara:copybara"
