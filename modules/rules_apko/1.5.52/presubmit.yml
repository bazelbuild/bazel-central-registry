bcr_test_module:
  module_path: "e2e/smoke"
  matrix:
    bazel: ["7.x", "8.x", "9.x"]
    platform: ["debian13", "ubuntu2204", "ubuntu2404"]
  tasks:
    run_tests:
      name: "Run test module"
      bazel: ${{ bazel }}
      platform: ${{ platform }}
      test_targets:
        - "//..."
