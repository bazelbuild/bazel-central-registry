load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "http_transport_test",
    srcs = ["http_transport_test.c"],
    local_defines = select({
        "//config:transport_compression_enabled": ["TEST_EXPECT_COMPRESSION"],
        "//conditions:default": [],
    }),
    target_compatible_with = select({
        "//config:transport_curl_enabled": [],
        "//conditions:default": ["@platforms//:incompatible"],
    }),
    deps = [
        "//:sentry",
        "@zlib",
    ],
)
