bcr_test_module:
  module_path: "test"
  matrix:
    platform: ["debian10", "macos", "ubuntu2004"] # "windows" doesn't work until we get toml2json prebuilts
    bazel: ["rolling"] # "8.x" doesn't have Fabian's downloader race fix yet
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
