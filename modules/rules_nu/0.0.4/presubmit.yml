bcr_test_module:
  module_path: "."
  matrix:
    platform: ["macos_arm64", "ubuntu2004"]
    bazel: ["8.x", "9.x"]
  tasks:
    verify_targets:
      name: Verify build targets
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "@rules_nu//..."
