matrix:
  platform: ["ubuntu2004", "macos", "windows"]
  bazel: ["9.x"]
tasks:
  verify_targets:
    name: "Verify Go proto targets"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rules_go_write_protos//pkg/foo:foo_go_proto"
