module(
    name = "mimalloc",
    version = "2.1.7-bcr.alpha.1",
    bazel_compatibility = [
        # Requires overlay support.
        ">=7.2.1",
    ],
)

bazel_dep(name = "platforms", version = "0.0.10")
