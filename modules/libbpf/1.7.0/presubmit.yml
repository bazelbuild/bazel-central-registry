bcr_test_module:
  module_path: ""
  matrix:
    platform:
      - debian11
      - ubuntu2204
    bazel: [7.x, 8.x]
  tasks:
    build_targets:
      name: Build libbpf
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "@libbpf//:libbpf_impl"
        - "@libbpf//:libbpf"
