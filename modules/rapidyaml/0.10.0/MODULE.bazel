module(
    name = "rapidyaml",
    version = "0.10.0",
    bazel_compatibility = [">=7.2.1"],  # need support for "overlay" directory
    compatibility_level = 0,
)

bazel_dep(name = "c4core", version = "0.2.7")
bazel_dep(name = "rules_cc", version = "0.2.13")
