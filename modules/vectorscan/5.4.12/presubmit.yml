matrix:
  platform:
  - ubuntu2404
  - ubuntu2404_arm64
  bazel: ["8.x", "9.x"]

tasks:
  verify_targets:
    name: Build and test Vectorscan
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - "@vectorscan"
    test_targets:
    - "@vectorscan//:unit_hyperscan"
