diff --git a/BUILD.bazel b/BUILD.bazel
new file mode 100644
index 0000000..2a6516d
--- /dev/null
+++ b/BUILD.bazel
@@ -0,0 +1,8 @@
+load("@boost.rules.tools//:tools.bzl", "boost_library")
+
+boost_library(
+    name = "assert",
+    deps = [
+        "@boost.config//:config",
+    ],
+)
diff --git a/MODULE.bazel b/MODULE.bazel
new file mode 100755
index 0000000..d0f688f
--- /dev/null
+++ b/MODULE.bazel
@@ -0,0 +1,13 @@
+module(
+    name = "boost.assert",
+    version = "1.83.0.bzl.1",
+    compatibility_level = 1,
+)
+
+bazel_dep(name = "boost.rules.tools", version = "1.0.0")
+bazel_dep(name = "platforms", version = "0.0.8")
+bazel_dep(name = "boost.config", version = "1.83.0.bzl.1")
+
+# Test-only deps
+bazel_dep(name = "boost.core", version = "1.83.0.bzl.1")
+bazel_dep(name = "boost.detail", version = "1.83.0.bzl.1")
diff --git a/WORKSPACE.bazel b/WORKSPACE.bazel
new file mode 100644
index 0000000..be0754f
--- /dev/null
+++ b/WORKSPACE.bazel
@@ -0,0 +1 @@
+# SEE: MODULE.bazel
diff --git a/test/BUILD.bazel b/test/BUILD.bazel
new file mode 100644
index 0000000..e7b9608
--- /dev/null
+++ b/test/BUILD.bazel
@@ -0,0 +1,211 @@
+test_suite(
+    name = "tests",
+    tests = [
+        ":assert_exp_test",
+        ":assert_is_void_test",
+        ":assert_msg_exp_test",
+        ":assert_msg_test2",
+        ":assert_test",
+        ":assert_test2",
+        ":current_function_test",
+        ":current_function_test2",
+        ":source_location_test",
+        ":source_location_test2",
+        ":source_location_test3",
+        ":source_location_test4",
+        ":source_location_test5",
+        ":source_location_test6",
+        ":verify_exp_test",
+        ":verify_msg_exp_test",
+        ":verify_test",
+    ],
+)
+
+cc_test(
+    name = "assert_is_void_test",
+    size = "small",
+    srcs = ["assert_is_void_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "assert_msg_test2",
+    size = "small",
+    srcs = ["assert_msg_test2.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "assert_test",
+    size = "small",
+    srcs = ["assert_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "assert_test2",
+    size = "small",
+    srcs = ["assert_test2.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "current_function_test",
+    size = "small",
+    srcs = ["current_function_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "current_function_test2",
+    size = "small",
+    srcs = ["current_function_test2.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+# Tests in the 'exp' subdirectory
+cc_test(
+    name = "assert_exp_test",
+    size = "small",
+    srcs = ["exp/assert_exp_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "assert_msg_exp_test",
+    size = "small",
+    srcs = ["exp/assert_msg_exp_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "verify_exp_test",
+    size = "small",
+    srcs = ["exp/verify_exp_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "verify_msg_exp_test",
+    size = "small",
+    srcs = ["exp/verify_msg_exp_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+# Source location tests
+cc_test(
+    name = "source_location_test",
+    size = "small",
+    srcs = ["source_location_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "source_location_test2",
+    size = "small",
+    srcs = ["source_location_test2.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "source_location_test3",
+    size = "small",
+    srcs = ["source_location_test3.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "source_location_test4",
+    size = "small",
+    srcs = ["source_location_test4.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "source_location_test5",
+    size = "small",
+    srcs = ["source_location_test5.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "source_location_test6",
+    size = "small",
+    srcs = ["source_location_test6.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
+
+cc_test(
+    name = "verify_test",
+    size = "small",
+    srcs = ["verify_test.cpp"],
+    deps = [
+        "@boost.assert//:assert",
+        "@boost.core//:core",
+        "@boost.detail//:detail",
+    ],
+)
