load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

cc_binary(
    name = "patchelf",
    srcs = glob([
        "src/*.cc",
        "src/*.h",
    ]),
    copts = [
        "-std=c++17",
    ],
    local_defines = [
        "_FILE_OFFSET_BITS=64",
    ],
    visibility = ["//visibility:public"],
)
