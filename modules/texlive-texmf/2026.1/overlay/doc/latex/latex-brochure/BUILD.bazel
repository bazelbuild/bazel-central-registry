# Empty package to shadow the upstream `build` file, which case-insensitive
# filesystems (macOS, Windows) interpret as a Bazel BUILD file.

package(default_visibility = ["//visibility:public"])

exports_files(glob(["**"]))
