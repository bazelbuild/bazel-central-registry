matrix:
  platform:
    - debian13
    - macos_arm64
    - ubuntu2004
    - windows
  bazel: ["7.x", "8.x", "9.x"]
tasks:
  verify_targets:
    name: "Verify filegroups"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - "@texlive-texmf//..."
