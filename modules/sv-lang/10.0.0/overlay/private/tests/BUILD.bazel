load("@bazel_skylib//rules:build_test.bzl", "build_test")

# Test builds of binaries and libraries
build_test(
    name = "test_slang_builds",
    targets = [
        "//:libsvlang",
        "//:slang",
        "//:slang-hier",
        "//:slang-reflect",
        "//:rewriter",
        "//:slang-tidy",
        "//:slang-threadtest",
        "//pyslang:pyslang_lib",
    ],
)


# TODO: wrap up the tests (C++ and Python) in the source repo to be run here as well
