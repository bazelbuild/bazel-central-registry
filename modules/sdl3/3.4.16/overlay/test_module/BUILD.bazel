load("@rules_cc//cc:defs.bzl", "cc_test")

# A consumer from outside the module: visibility, the archives a consumer links,
# and on macOS the Objective-C half and its frameworks. C++ exercises the
# headers' extern "C" guards.
cc_test(
    name = "smoke_test",
    size = "small",
    srcs = ["smoke.cc"],
    deps = ["@sdl3"],
)
