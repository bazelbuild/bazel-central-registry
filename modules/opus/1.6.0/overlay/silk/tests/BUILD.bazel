load("@rules_cc//cc:defs.bzl", "cc_test")
load("//:defs.bzl", "OPUS_COPTS")

package(default_visibility = ["//visibility:public"])

cc_test(
    name = "test_unit_LPC_inv_pred_gain",
    srcs = [
        "test_unit_LPC_inv_pred_gain.c",
        "//:silk/LPC_inv_pred_gain.c",
    ],
    copts = OPUS_COPTS,
    deps = ["//:opus"],
)
