matrix:
  platform:
    - macos
    - ubuntu2004
  bazel:
    - "7.x"
    - "8.x"
tasks:
  run_tests:
    bazel: ${{ bazel }}
    platform: ${{ platform }}
    build_targets:
    - "@rules_closure//..."

