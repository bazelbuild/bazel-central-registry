bcr_test_module:
  module_path: "."
  matrix:
    platform:
      - debian10
      - macos
      - ubuntu2004
  tasks:
    run_tests:
      platform: ${{ platform }}
      build_targets:
      - "//..."
      test_targets:
      - "//..."
