bcr_test_module:
  module_path: "tests/bcr-consumer"
  matrix:
    platform:
    - debian10
    - ubuntu2004
    - ubuntu2004_arm64
    - macos
    - macos_arm64
    - windows
    bazel: ["8.x", "9.x"]
  tasks:
    run_tests:
      name: "Run bcr-consumer test"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
      - "//..."
