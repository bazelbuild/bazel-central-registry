matrix:
  platform:
  - debian13
  - ubuntu2404
  - macos_arm64
  - windows
  bazel:
  - "8.*"
  - "9.*"
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@blas//:blas'
