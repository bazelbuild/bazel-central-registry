package(default_visibility = ["//visibility:public"])

# Exported individually so consumers can use `$(rlocationpath
# @prjoxide-db//:devices.json)` to discover the db root at runtime.
exports_files(["devices.json"])

filegroup(
    name = "prjoxide-db",
    srcs = glob(["**/*"], exclude = ["**/*.bazel"]),
)
