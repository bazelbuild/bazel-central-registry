bcr_test_module:
  module_path: ""
  matrix:
    platform: ["debian10", "macos", "ubuntu2004"]
    bazel: ["8.x"]
  tasks:
    run_build:
      name: "Build"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
