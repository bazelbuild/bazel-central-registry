bcr_test_module:
  module_path: ""
  matrix:
    platform: ["macos_arm64", "ubuntu2204"]
    bazel: ["8.x"]
  tasks:
    run_build:
      name: "Build"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
