matrix:
  platform: ["debian10", "ubuntu2004"]
  bazel: ["6.x", "7.x"]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_flags:
    - '--cxxopt=-std=c++17'
    build_targets:
    - '@tcmalloc//tcmalloc'
