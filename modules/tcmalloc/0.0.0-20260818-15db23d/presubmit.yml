matrix:
  platform: ["debian13"]
  bazel: ["8.x", "9.x"]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    environment:
      CC: clang
    build_flags:
    - '--cxxopt=-std=c++17'
    - '--host_cxxopt=-std=c++17'
    build_targets:
    - '@tcmalloc//tcmalloc'
