matrix:
  platform: ["debian13"]
  bazel: ["8.x", "9.x"]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    environment:
      CC: clang
    build_flags:
    - '--cxxopt=-std=c++17'
    - '--copt=-Wno-c++20-extensions'
    build_targets:
    - '@tcmalloc//tcmalloc'
