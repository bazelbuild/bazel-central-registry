bcr_test_module:
  module_path: tests/e2e/always_build
  matrix:
    bazel:
      - 8.x
      - 9.x
    platform:
      - debian11
      - ubuntu2204
      - macos_arm64
  tasks:
    run_tests:
      name: Run test module
      bazel: ${{ bazel }}
      platform: ${{ platform }}
      test_targets:
        - //...
