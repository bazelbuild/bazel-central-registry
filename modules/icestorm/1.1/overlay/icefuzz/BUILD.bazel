package(default_visibility = ["//:__subpackages__"])

filegroup(
    name = "timing_data",
    srcs = glob(["timings_*.txt"]),
)

# Individual timing files for use in genrules
exports_files(glob(["timings_*.txt"]))
