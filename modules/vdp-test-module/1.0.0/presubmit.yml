bcr_test_module:
  module_path: ""
  matrix:
    platform: ["ubuntu2004"]
    bazel: ["8.x"]
  tasks:
    verify_targets:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
      - "//..."
