matrix:
  platform:
  - debian11
  - ubuntu2404
  - macos
  bazel:
  - 8.x

tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - "@icu//..."
