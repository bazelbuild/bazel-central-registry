bcr_test_module:
  module_path: "e2e/smoke"
  matrix:
    platform: ["macos", "ubuntu2004"]
    bazel: ["8.x", "9.x", "rolling"]
  tasks:
    run_tests:
      name: "Run e2e tests"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
