bazel_dep(name = "opencensus-cpp", version = "0.0.0-20230502-50eb5de.bcr.3")
bazel_dep(name = "rules_cc", version = "0.2.17")

# Temporary root override for the grpc/opencensus-cpp Bazel 9 migration cycle.
# Remove this after grpc has a BCR release with these Bazel 9 fixes.
single_version_override(
    module_name = "grpc",
    patches = ["//:grpc_bazel9.patch"],
    patch_strip = 1,
    version = "1.80.0",
)
