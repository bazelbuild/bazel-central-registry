bcr_test_module:
  module_path: ""
  matrix:
    platform: ["macos_arm64", "ubuntu2004"]
    bazel: ["7.x", "8.x"]
  tasks:
    verify_targets:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
      - "//..."
      test_targets:
      - "//..."

bcr_test_module:
  module_path: test_gawk_llvm
  matrix:
    platform:
    - debian10
    - debian11
    - ubuntu2004
    - ubuntu2004_arm64
    - ubuntu2204
    - ubuntu2404
    - macos
    - macos_arm64
    bazel: [8.x, 9.x]
  tasks:
    run_test_gawk_llvm_module:
      name: Run test gawk llvm module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets: [//...]
      test_targets: [//...]
