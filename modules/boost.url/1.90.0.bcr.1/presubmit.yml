matrix:
  platform:
    - debian11
    - macos
    - macos_arm64
    - ubuntu2204
    - ubuntu2404
    - windows
  bazel: [7.x, 8.x, 9.x, rolling]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_flags:
      - "--process_headers_in_dependencies"
    build_targets:
      - "@boost.url//:boost.url"
    test_flags:
      - "--process_headers_in_dependencies"
    test_targets:
      - "@boost.url//:boost.url_test"
