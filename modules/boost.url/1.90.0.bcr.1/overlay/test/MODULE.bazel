bazel_dep(name = "boost.url")
local_path_override(
    module_name = "boost.url",
    path = "..",
)

bazel_dep(name = "boost.url.extra")
local_path_override(
    module_name = "boost.url.extra",
    path = "../extra",
)

bazel_dep(name = "rules_cc", version = "0.2.16")
