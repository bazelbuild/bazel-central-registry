matrix:
  platform:
    - debian11
    - ubuntu2204
    - macos
    - macos_arm64
    - windows
  bazel:
    - 7.x

tasks:
  build_wasm_binary:
    name: Build wasm_binary
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@emsdk//emscripten_toolchain:wasm_binary'

