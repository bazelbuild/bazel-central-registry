diff --git a/proxygen/httpserver/tests/HTTPServerTest.cpp b/proxygen/httpserver/tests/HTTPServerTest.cpp
index b4f6b44..4866c17 100644
--- a/proxygen/httpserver/tests/HTTPServerTest.cpp
+++ b/proxygen/httpserver/tests/HTTPServerTest.cpp
@@ -41,7 +41,7 @@ using namespace CurlService;
 
 namespace {
 
-const std::string kTestDir = getContainingDirectory(XLOG_FILENAME).str();
+const std::string kTestDir = getContainingDirectory(BAZEL_TEST_RPATH).str();
 } // namespace
 
 class ServerThread {
diff --git a/proxygen/lib/http/coro/client/test/HTTPClientTests.cpp b/proxygen/lib/http/coro/client/test/HTTPClientTests.cpp
index 30ac1df..c395bb3 100644
--- a/proxygen/lib/http/coro/client/test/HTTPClientTests.cpp
+++ b/proxygen/lib/http/coro/client/test/HTTPClientTests.cpp
@@ -1773,9 +1773,9 @@ class CertReloadSessionPoolTests : public HTTPClientTests {
 // scenario where long-running services need to refresh expiring certs.
 CO_TEST_P_X(CertReloadSessionPoolTests, TimerCallback) {
   auto certPath =
-      getContainingDirectory(__FILE__).str() + "/certs/test_cert1.pem";
+      folly::test::find_resource("proxygen/lib/http/coro/client/test/certs/test_cert1.pem").string();
   auto keyPath =
-      getContainingDirectory(__FILE__).str() + "/certs/test_key1.pem";
+      folly::test::find_resource("proxygen/lib/http/coro/client/test/certs/test_key1.pem").string();
   auto transportType = GetParam();
 
   // Capture base connection params so we can preserve settings like the
diff --git a/proxygen/lib/http/coro/client/test/HTTPClientTestsCommon.cpp b/proxygen/lib/http/coro/client/test/HTTPClientTestsCommon.cpp
index 717b8d2..69026fc 100644
--- a/proxygen/lib/http/coro/client/test/HTTPClientTestsCommon.cpp
+++ b/proxygen/lib/http/coro/client/test/HTTPClientTestsCommon.cpp
@@ -8,6 +8,8 @@
 
 #include "proxygen/lib/http/coro/client/test/HTTPClientTestsCommon.h"
 
+#include <folly/testing/TestUtil.h>
+
 using folly::coro::co_nothrow;
 
 namespace proxygen::coro::test {
@@ -22,10 +24,14 @@ namespace proxygen::coro::test {
       folly::SSLContext::VerifyClientCertificate::DO_NOT_REQUEST;
   tlsConfig.setNextProtocols({"h2", "http/1.1"});
   try {
-    const std::string kTestDir = getContainingDirectory(XLOG_FILENAME).str();
-    tlsConfig.setCertificate(kTestDir + "certs/test_cert1.pem",
-                             kTestDir + "certs/test_key1.pem",
-                             "");
+    tlsConfig.setCertificate(
+        folly::test::find_resource(
+            "proxygen/lib/http/coro/client/test/certs/test_cert1.pem")
+            .string(),
+        folly::test::find_resource(
+            "proxygen/lib/http/coro/client/test/certs/test_key1.pem")
+            .string(),
+        "");
   } catch (const std::exception& ex) {
     XLOG(ERR) << "Invalid certificate file or key file: %s" << ex.what();
   }
diff --git a/proxygen/lib/http/session/test/HTTPSessionAcceptorTest.cpp b/proxygen/lib/http/session/test/HTTPSessionAcceptorTest.cpp
index 4d54db6..d225bb2 100644
--- a/proxygen/lib/http/session/test/HTTPSessionAcceptorTest.cpp
+++ b/proxygen/lib/http/session/test/HTTPSessionAcceptorTest.cpp
@@ -27,7 +27,7 @@ using folly::test::MockAsyncSocket;
 
 namespace {
 
-const std::string kTestDir = getContainingDirectory(XLOG_FILENAME).str();
+const std::string kTestDir = getContainingDirectory(BAZEL_TEST_RPATH).str();
 
 }
 
