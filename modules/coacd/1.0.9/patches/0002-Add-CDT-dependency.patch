diff --git a/BUILD.bazel b/BUILD.bazel
index 202a92a..3321300 100644
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -1,72 +1,29 @@
 load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library")
 
-SHARED_COPTS = select({
-    "@platforms//os:windows": [
-        "/std:c++20",
-        "/bigobj",
-        "/W0",
-        "/openmp",
-    ],
-    "//conditions:default": [
-        "-std=c++20",
-        "-Wall",
-        "-fpermissive",
-        "-Wno-error=stringop-overflow",
-        "-Wno-int-in-bool-context",
-        "-Wsign-compare",
-        "-fopenmp",
-    ],
-})
-
-SHARED_LINKOPTS = select({
-    "@platforms//os:windows": [],
-    "//conditions:default": [
-        "-fopenmp",
-        "-pthread",
-    ],
-})
-
 cc_library(
     name = "coacd_lib",
-    srcs = glob([
-        "src/**/*.cc",
-        "src/**/*.cpp",
-        "public/coacd.cpp",
-    ], allow_empty = True),
-    hdrs = glob([
-        "public/**/*.h",
-        "src/**/*.h",
-        "src/**/*.hpp",
-        "3rd/cdt/CDT/**/*.h",
-        "3rd/cdt/CDT/**/*.hpp",
-    ], allow_empty = True),
-    includes = [
-        "public",
-        "3rd/cdt/CDT",
-    ],
+    srcs = glob(["src/**/*.cpp"], exclude = ["src/main.cpp"]),
+    hdrs = glob(["src/**/*.h", "src/**/*.hpp", "public/*.h"]),
+    includes = ["src", "public"],
+    copts = ["-std=c++20"],
     defines = [
+        "COACD_USE_OPENVDB",
         "WITH_3RD_PARTY_LIBS=1",
         "_USE_MATH_DEFINES",
     ],
-    copts = SHARED_COPTS,
-    linkopts = SHARED_LINKOPTS,
+    visibility = ["//visibility:public"],
     deps = [
         "@openvdb//:openvdb",
+        "@onetbb//:tbb",
         "@spdlog//:spdlog",
         "@fmt//:fmt",
+        "@cdt",
     ],
-    visibility = ["//visibility:public"],
 )
 
 cc_binary(
     name = "main",
     srcs = ["main.cpp"],
-    copts = SHARED_COPTS,
-    linkopts = SHARED_LINKOPTS,
-    deps = [
-        ":coacd_lib",
-        "@openvdb//:openvdb",
-        "@spdlog//:spdlog",
-        "@fmt//:fmt",
-    ],
+    copts = ["-std=c++20"],
+    deps = [":coacd_lib"],
 )
diff --git a/MODULE.bazel b/MODULE.bazel
index 4dc1807..1adb522 100644
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -12,3 +12,6 @@ bazel_dep(name = "spdlog", version = "1.14.1")
 bazel_dep(name = "fmt", version = "11.0.2")
 bazel_dep(name = "onetbb", version = "2022.0.0")
 bazel_dep(name = "zlib", version = "1.3.1.bcr.5")
+
+cdt_ext = use_extension("//:coacd_ext.bzl", "cdt_ext")
+use_repo(cdt_ext, "cdt")
diff --git a/coacd_ext.bzl b/coacd_ext.bzl
new file mode 100644
index 0000000..7f76291
--- /dev/null
+++ b/coacd_ext.bzl
@@ -0,0 +1,19 @@
+load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")
+
+def _cdt_ext_impl(ctx):
+    http_archive(
+        name = "cdt",
+        build_file_content = """
+load("@rules_cc//cc:defs.bzl", "cc_library")
+cc_library(
+    name = "cdt",
+    hdrs = glob(["CDT/include/**/*.h", "CDT/include/**/*.hpp", "CDT/include/**/*.inl"], allow_empty = True),
+    includes = ["CDT"],
+    visibility = ["//visibility:public"],
+)
+""",
+        strip_prefix = "CDT-1.4.1",
+        urls = ["https://github.com/artem-ogre/CDT/archive/refs/tags/1.4.1.tar.gz"],
+    )
+
+cdt_ext = module_extension(implementation = _cdt_ext_impl)
