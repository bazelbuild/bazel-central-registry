"""
Test module for NLopt.
"""

module(
    name = "nlopt_test",
    version = "0.0.0",
    bazel_compatibility = [">=7.6.0"],
    compatibility_level = 0,
)

bazel_dep(name = "nlopt", version = "2.7.1")
local_path_override(
    module_name = "nlopt",
    path = "..",
)

bazel_dep(name = "platforms", version = "1.0.0")
