load("//contrib/bazel:defs.bzl", "GIT_COPTS")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "compiler-tricks",
    srcs = ["not-constant.c"],
    copts = GIT_COPTS,
    deps = ["//:git_compat_util"],
)
