load("//bazel:texlive.bzl", "FLEX_MSVC_DEFINES", "texk_program")

texk_program(
    name = "detex",
    srcs = glob(["detex-src/*.c"]),
    hdrs = glob(["detex-src/*.h"]),
    local_defines = ["VERSION=\\\"2.8\\\""] + FLEX_MSVC_DEFINES,
)
