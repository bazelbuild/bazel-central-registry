"""vlna - add ties after non-syllabic prepositions in Czech/Slovak TeX."""

load("@rules_cc//cc:cc_binary.bzl", "cc_binary")
load("//bazel:texlive.bzl", "TEXLIVE_COPTS")

cc_binary(
    name = "vlna",
    srcs = ["vlna.c"],
    copts = TEXLIVE_COPTS,
    visibility = ["//visibility:public"],
)
