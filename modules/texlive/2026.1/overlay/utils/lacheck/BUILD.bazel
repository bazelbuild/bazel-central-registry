"""lacheck - LaTeX checker."""

load("@rules_cc//cc:cc_binary.bzl", "cc_binary")
load("//bazel:texlive.bzl", "FLEX_MSVC_DEFINES", "TEXLIVE_COPTS")

cc_binary(
    name = "lacheck",
    srcs = ["lacheck.c"],
    copts = TEXLIVE_COPTS,
    local_defines = FLEX_MSVC_DEFINES,
    visibility = ["//visibility:public"],
)
