bcr_test_module:
  module_path: "e2e/bzlmod"
  matrix:
    platform: ["debian10", "macos", "ubuntu2004"]
    bazel: ["8.x", "9.x"]
  tasks:
    run_tests:
      name: "Run Bzlmod consumer tests"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
