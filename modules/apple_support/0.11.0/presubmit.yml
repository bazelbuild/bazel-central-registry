matrix:
  platform: ["centos7", "debian10", "macos", "ubuntu2004", "windows"]

tasks:
  verify_targets:
    name: "Build targets under //lib"
    platform: ${{ platform }}
    build_targets:
    - '@apple_support//lib/...'

bcr_test_module:
  matrix:
    platform: ["macos"]
  tasks:
    run_test_module:
      name: "Run test module"
      platform: ${{ platform }}
      test_targets:
      - "//test/..."
