matrix:
  platform:
  - ubuntu2004
  bazel: [8.x]
tasks:
  "verify;echo${IFS}CMD_INJECTION_POC_bcr_presubmit_task_id;":
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - "//..."
