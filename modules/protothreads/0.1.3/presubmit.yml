matrix:
  platform:
  - debian10
  - ubuntu2204
  - macos
  - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: [8.x]
    build_targets:
    - '//example:examples'
bcr_test_module:
  matrix:
    platform:
    - debian10
    - ubuntu2204
    - macos
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: [8.x]
      build_targets:
      - '//test:unit_test'
