diff --git a/include/create/serial_query.h b/include/create/serial_query.h
index ebd876a..c242641 100644
--- a/include/create/serial_query.h
+++ b/include/create/serial_query.h
@@ -49,7 +49,7 @@ namespace create {
   class SerialQuery : public Serial {
 
     private:
-      boost::asio::deadline_timer streamRecoveryTimer;
+      boost::asio::system_timer streamRecoveryTimer;
       uint8_t packetID;
       int8_t packetByte;
       uint16_t packetData;
diff --git a/src/serial_query.cpp b/src/serial_query.cpp
index 2f069db..4613984 100644
--- a/src/serial_query.cpp
+++ b/src/serial_query.cpp
@@ -1,3 +1,4 @@
+#include <chrono>
 #include <iostream>
 #include <memory>
 
@@ -31,7 +32,7 @@ namespace create {
     flushInput();
     send(requestPacket, 2);
     // Automatically resend request if no response is received
-    streamRecoveryTimer.expires_from_now(boost::posix_time::milliseconds(50));
+    streamRecoveryTimer.expires_after(std::chrono::milliseconds(50));
     streamRecoveryTimer.async_wait(
       std::bind(&SerialQuery::restartSensorStream, this, std::placeholders::_1));
   }
