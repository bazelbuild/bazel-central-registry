# Automatically apply OS-named configs (macos, linux, windows)
common --enable_platform_specific_config

# std::filesystem requires macOS 10.15+; set minimum for both target and host tools
build:macos --copt=-mmacosx-version-min=10.15
build:macos --host_copt=-mmacosx-version-min=10.15
