--- MODULE.bazel
+++ MODULE.bazel
@@ -60,7 +60,7 @@
 crate.from_cargo(
     name = "crate_index",
     cargo_lockfile = "//:Cargo.lock",
-    lockfile = "//:Cargo.Bazel.lock",
+    # lockfile = "//:Cargo.Bazel.lock",
     manifests = [
         "//:Cargo.toml",
         "//:benchmarks/event/Cargo.toml",
