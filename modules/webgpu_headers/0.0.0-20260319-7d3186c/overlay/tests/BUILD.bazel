load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "webgpu_header_smoke",
    srcs = ["webgpu_header_smoke.cc"],
    deps = ["@webgpu_headers"],
)

genrule(
    name = "webgpu_schema_assets_smoke",
    srcs = [
        "@webgpu_headers//:schema_json",
        "@webgpu_headers//:webgpu_json",
    ],
    outs = ["webgpu_schema_assets_smoke.txt"],
    cmd = "cat $(SRCS) > $@",
)
