matrix:
  platform:
    - debian11
    - ubuntu2204
    - macos
    - macos_arm64
    - windows
  bazel:
    - 7.x
    - 8.x
    - 9.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@orocos-kdl//:orocos-kdl'
bcr_test_module:
  module_path: tests
  matrix:
    platform:
      - debian11
      - ubuntu2204
      - macos
      - macos_arm64
      - windows
    bazel:
      - 7.x
      - 8.x
      - 9.x
  tasks:
    run_tests:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //...
      test_targets:
        - //...
