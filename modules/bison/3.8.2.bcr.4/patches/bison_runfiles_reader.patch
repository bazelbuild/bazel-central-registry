diff --git a/src/reader.c b/src/reader.c
--- a/src/reader.c
+++ b/src/reader.c
@@ -757,13 +757,23 @@
 void
 reader (const char *gram)
 {
+  fprintf (stderr, "BISON_RUNFILES_DEBUG: reader: enter, gram=%s\n", gram ? gram : "(null)");
+  fflush (stderr);
   /* Set up symbol_table, semantic_type_table, and the built-in
      symbols.  */
   symbols_new ();
+  fprintf (stderr, "BISON_RUNFILES_DEBUG: reader: after symbols_new\n");
+  fflush (stderr);
 
   gram_scanner_open (gram);
+  fprintf (stderr, "BISON_RUNFILES_DEBUG: reader: after gram_scanner_open\n");
+  fflush (stderr);
   parser_init ();
+  fprintf (stderr, "BISON_RUNFILES_DEBUG: reader: after parser_init\n");
+  fflush (stderr);
   gram_parse ();
+  fprintf (stderr, "BISON_RUNFILES_DEBUG: reader: after gram_parse\n");
+  fflush (stderr);
   gram_scanner_close ();
 
   prepare_percent_define_front_end_variables ();
