--- a/lib/windows-spawn.c
+++ b/lib/windows-spawn.c
@@ -561,6 +561,7 @@
       break;
 
     default:
+      fprintf (stderr, "BISON_RUNFILES_DEBUG: CreateProcess failed, Windows error code=%lu (0x%lx)\n", (unsigned long)error, (unsigned long)error);
       return EINVAL;
       break;
     }
