"Bazel dependencies"

module(
    name = "rules_mise",
    version = "0.5.0",
)

bazel_dep(name = "bazel_skylib", version = "1.9.0")
bazel_dep(name = "package_metadata", version = "0.0.7")
bazel_dep(name = "platforms", version = "1.0.0")
bazel_dep(name = "rules_cc", version = "0.2.18")
bazel_dep(name = "toml.bzl", version = "0.3.0")
bazel_dep(name = "bazel_lib", version = "3.2.2")
bazel_dep(name = "bazel_features", version = "1.45.0")

bazel_dep(name = "gazelle", version = "0.50.0", dev_dependency = True, repo_name = "bazel_gazelle")
bazel_dep(name = "bazel_skylib_gazelle_plugin", version = "1.9.0", dev_dependency = True)

bazel_dep(name = "bazelrc-preset.bzl", version = "1.6.0")

# Extension is available for consumers who want to use mise.hub()
# buildifier: disable=unused-variable
# mise = use_extension("//mise:extensions.bzl", "mise")
