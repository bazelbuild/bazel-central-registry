bcr_test_module:
  module_path: ""
  matrix:
    platform: [ubuntu2404]
    bazel: [7.x, 8.x, 9.x]
  tasks:
    run_tests:
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets: [//...]
