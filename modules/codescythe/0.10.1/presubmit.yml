matrix:
  platform:
    - macos_arm64
    - ubuntu2204
    - ubuntu2204_arm64
  bazel: ["8.x", "9.x"]
tasks:
  build_binary:
    name: Build Codescythe binary
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@codescythe//:codescythe"
