bcr_test_module:
  module_path: .
  matrix:
    bazel:
      - 7.x
    platform:
      - debian11
      - ubuntu2204
      - macos
      - macos_arm64
      - windows
  tasks:
    build:
      name: Build Drake targets
      bazel: ${{ bazel }}
      platform: ${{ platform }}
      build_targets:
        - "@drake//:some_test_models"