matrix:
  platform:
  - debian11
  - ubuntu2204
  - windows
  bazel:
  - 9.x
  - 8.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@rules_elisp//elisp/...'
bcr_test_module:
  module_path: examples/ext
  matrix:
    platform:
    - debian11
    - ubuntu2204
    - windows
    bazel:
    - 9.x
    - 8.x
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
      - //...
      test_targets:
      - //...
