--- /dev/null
+++ lib/fstree/BUILD.bazel
@@ -0,0 +1,20 @@
+load("@rules_cc//cc:defs.bzl", "cc_library")
+load("//:defines.bzl", "DEFINES")
+load("//:pthread.bzl", "COPTS")
+
+cc_library(
+    name = "fstree",
+    srcs = glob(
+        [
+            "**/*.c",
+            "**/*.h",
+        ],
+    ) + ["//:config"],
+    copts = COPTS,
+    linkstatic = True,
+    local_defines = DEFINES,
+    visibility = ["//:__subpackages__"],
+    deps = [
+        "//:hdrs",
+    ],
+)
