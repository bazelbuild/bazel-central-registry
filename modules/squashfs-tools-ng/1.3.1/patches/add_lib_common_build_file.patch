--- /dev/null
+++ lib/common/BUILD.bazel
@@ -0,0 +1,21 @@
+load("@rules_cc//cc:defs.bzl", "cc_library")
+load("//:defines.bzl", "DEFINES")
+
+cc_library(
+    name = "common",
+    srcs = glob(
+        [
+            "**/*.c",
+            "**/*.h",
+        ],
+        exclude = [
+            "comp_lzo.c",  # "WITH_LZO"
+        ],
+    ) + ["//:config"],
+    linkstatic = True,
+    local_defines = DEFINES,
+    visibility = ["//:__subpackages__"],
+    deps = [
+        "//:hdrs",
+    ],
+)
