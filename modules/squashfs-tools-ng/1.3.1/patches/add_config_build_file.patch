--- /dev/null
+++ config/BUILD.bazel
@@ -0,0 +1,27 @@
+config_setting(
+    name = "x86_64-linux",
+    constraint_values = [
+        "@platforms//cpu:x86_64",
+        "@platforms//os:linux",
+    ],
+)
+
+config_setting(
+    name = "aarch64-linux",
+    constraint_values = [
+        "@platforms//cpu:arm64",
+        "@platforms//os:linux",
+    ],
+)
+
+alias(
+    name = "config",
+    actual = select(
+        {
+            "x86_64-linux": "x86_64-linux-gnu/config.h",
+            "aarch64-linux": "aarch64-linux-gnu/config.h",
+        },
+        no_match_error = "No pre-configured `config.h` available",
+    ),
+    visibility = ["//:__subpackages__"],
+)
