bcr_test_module:
  module_path: "."
  matrix:
    platform: ["debian10", "macos", "ubuntu2004"]
    bazel: ["8.x", "9.x", "rolling"]
  tasks:
    build_module:
      name: "Build module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
