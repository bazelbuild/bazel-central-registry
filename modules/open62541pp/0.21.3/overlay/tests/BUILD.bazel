load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "open62541pp_tests",
    srcs = glob(["*.cpp","helper/*.hpp"]),
    deps = [
        "@open62541pp//:open62541pp",
        "@open62541//:open62541",
        "@catch2//:catch2_main",
        "@openssl//:crypto"
    ],
)
