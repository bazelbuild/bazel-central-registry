matrix:
  platform:
    - macos
    - macos_arm64
  bazel: [7.x, 8.x, 9.x, rolling]
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@xctestrunner//:ios_test_runner'
