bcr_test_module:
  module_path: "e2e/bzlmod"
  matrix:
    platform: ["debian11", "ubuntu2004", "macos", "macos_arm64", "windows"]
    bazel: [8.x, 9.x]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
