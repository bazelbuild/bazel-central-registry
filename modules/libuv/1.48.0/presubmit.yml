matrix:
  bazel: ["7.x"]
tasks:
  verify_targets:
    platform: ubuntu2204
    bazel: ${{ bazel }}
    build_targets:
      - '@libuv//...'
    test_targets:
      - '@libuv//:tests'

  verify_targets_macos:
    platform: macos
    bazel: ${{ bazel }}
    build_targets:
      - '@libuv//...'
