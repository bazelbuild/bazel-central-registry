matrix:
  platform:
  - debian10
  - macos
  - macos_arm64
  - rockylinux8
  - ubuntu2004
  - windows
  bazel: 
  - 7.x
  - 8.x
  - 9.x
  - rolling
tasks:
  run_test:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@gflags//:gflags'
