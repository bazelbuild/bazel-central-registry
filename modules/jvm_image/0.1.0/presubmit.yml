bcr_test_module:
  module_path: "example/hello"
  matrix:
    platform: ["macos", "ubuntu2204"]
    bazel: ["8.*", "9.*"]
  tasks:
    build_example:
      name: "Build example image"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//:image"
