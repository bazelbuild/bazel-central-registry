bcr_test_module:
  module_path: "."
  matrix:
    platform:
      - ubuntu2204
    bazel:
      - 8.x
  tasks:
    test:
      name: Test Prismo
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
