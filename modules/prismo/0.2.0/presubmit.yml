bcr_test_module:
  module_path: "."
  matrix:
    platform:
      - ubuntu2204
    bazel:
      - 8.x
  tasks:
    build:
      name: Build Prismo
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
    test:
      name: Test Prismo
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
