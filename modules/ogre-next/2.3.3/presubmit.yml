matrix:
  platform:
  - debian10
  - ubuntu2004
  - macos
  - macos_arm64
  bazel:
  - 8.x
  - 7.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@ogre-next//:ogre-next-gl3plus'
    - '@ogre-next//:hlms_common'
    - '@ogre-next//:hlms_pbs'
    - '@ogre-next//:hlms_unlit'
    - '@ogre-next//:overlay'
