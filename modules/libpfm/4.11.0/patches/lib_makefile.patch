--- lib/Makefile
+++ lib/Makefile
@@ -379,9 +380,14 @@
 
 $(OBJS) $(SOBJS): $(TOPDIR)/config.mk $(TOPDIR)/rules.mk Makefile $(INCDEP)
 
+AR_CMDLINE=cq
+ifeq ($(SYS),Darwin)
+AR_CMDLINE=-static -o
+endif
+
 libpfm.a:  $(OBJS)
 	$(RM) $@
-	$(AR) cq $@ $(OBJS)
+	$(AR) $(AR_CMDLINE) $@ $(OBJS)
 
 $(SLIBPFM):  $(SOBJS)
 	$(CC) $(CFLAGS) $(SLDFLAGS)  -o $@ $(SOBJS)
