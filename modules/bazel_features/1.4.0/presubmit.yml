matrix:
  platform:
    - centos7
    - debian10
    - ubuntu2004
    - macos
    - windows
tasks:
  verify_targets:
    name: Verify build targets
    bazel: [6.x, 7.x]
    platform: ${{ platform }}
    build_targets:
      - "@bazel_features//test:tests"
