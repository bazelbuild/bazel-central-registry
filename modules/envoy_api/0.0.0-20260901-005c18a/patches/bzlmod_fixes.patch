diff --git a/bazel/envoy_http_archive.bzl b/bazel/envoy_http_archive.bzl
--- a/bazel/envoy_http_archive.bzl
+++ b/bazel/envoy_http_archive.bzl
@@ -11,6 +11,7 @@
         # wants to override the version. Do nothing.
         return
     location = locations[location_name or name]
+    kwargs.pop("repo_mapping", None)
 
     # HTTP tarball at a given URL. Add a BUILD file if requested.
     http_archive(
diff --git a/bazel/api_build_system.bzl b/bazel/api_build_system.bzl
--- a/bazel/api_build_system.bzl
+++ b/bazel/api_build_system.bzl
@@ -103,8 +103,6 @@
             "@googleapis//google/rpc:status_cc_proto",
         ],
         deps = [relative_name],
-        protobuf = "@protobuf//:protobuf",
-        re2 = "@re2",
         visibility = ["//visibility:public"],
     )
 
