matrix:
  platform:
    - ubuntu2004
    - macos
    - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: 7.x
    build_targets:
      - "@foxglove_ws_protocol//..."
