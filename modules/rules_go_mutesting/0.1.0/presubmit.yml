bcr_test_module:
  module_path: "e2e/bcr_test"
  matrix:
    platform: ["debian11", "macos", "ubuntu2004"]
    bazel: ["8.x", "9.x"]
  tasks:
    run_tests:
      name: "Mutation testing works for a consuming module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
      build_targets:
        - "//..."
