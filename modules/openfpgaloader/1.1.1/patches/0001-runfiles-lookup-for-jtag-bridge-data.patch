diff --git a/src/altera.cpp b/src/altera.cpp
index 8b5d098..0eb4ea0 100644
--- a/src/altera.cpp
+++ b/src/altera.cpp
@@ -11,6 +11,7 @@
 #include <string>
 
 #include "common.hpp"
+#include "dataResolver.hpp"
 #include "device.hpp"
 #include "epcq.hpp"
 #include "jtag.hpp"
@@ -197,11 +198,12 @@ bool Altera::load_bridge()
 			return false;
 		}
 
-		bitname = get_shell_env_var("OPENFPGALOADER_SOJ_DIR", DATA_DIR "/openFPGALoader");
 #ifdef HAS_ZLIB
-		bitname += "/spiOverJtag_" + _device_package + ".rbf.gz";
+		bitname = resolveDataFile("spiOverJtag",
+			"spiOverJtag_" + _device_package + ".rbf.gz");
 #else
-		bitname += "/spiOverJtag_" + _device_package + ".rbf";
+		bitname = resolveDataFile("spiOverJtag",
+			"spiOverJtag_" + _device_package + ".rbf");
 #endif
 	}
 
diff --git a/src/efinix.cpp b/src/efinix.cpp
index 669133b..9ae7395 100644
--- a/src/efinix.cpp
+++ b/src/efinix.cpp
@@ -14,6 +14,7 @@
 #include <string>
 
 #include "common.hpp"
+#include "dataResolver.hpp"
 #include "device.hpp"
 #include "display.hpp"
 #include "efinixHexParser.hpp"
@@ -436,9 +437,8 @@ bool Efinix::prepare_flash_access()
 			return false;
 		}
 
-		bitname = get_shell_env_var("OPENFPGALOADER_SOJ_DIR",
-			DATA_DIR "/openFPGALoader");
-		bitname += "/spiOverJtag_efinix_" + _device_package + ".bit.gz";
+		bitname = resolveDataFile("spiOverJtag",
+			"spiOverJtag_efinix_" + _device_package + ".bit.gz");
 	}
 
 #if defined (_WIN64) || defined (_WIN32)
diff --git a/src/xilinx.cpp b/src/xilinx.cpp
index 549fe00..685a30a 100644
--- a/src/xilinx.cpp
+++ b/src/xilinx.cpp
@@ -20,6 +20,7 @@
 #include "bitparser.hpp"
 #include "common.hpp"
 #include "configBitstreamParser.hpp"
+#include "dataResolver.hpp"
 #include "display.hpp"
 #include "jedParser.hpp"
 #include "jtag.hpp"
@@ -724,8 +725,8 @@ bool Xilinx::load_bridge()
 			return false;
 		}
 
-		bitname = get_shell_env_var("OPENFPGALOADER_SOJ_DIR", DATA_DIR "/openFPGALoader");
-		bitname += "/spiOverJtag_" + _device_package + ".bit.gz";
+		bitname = resolveDataFile("spiOverJtag",
+			"spiOverJtag_" + _device_package + ".bit.gz");
 	}
 
 #if defined (_WIN64) || defined (_WIN32)
@@ -760,8 +761,8 @@ bool Xilinx::load_bpi_bridge()
 		return false;
 	}
 
-	bitname = get_shell_env_var("OPENFPGALOADER_SOJ_DIR", DATA_DIR "/openFPGALoader");
-	bitname += "/bpiOverJtag_" + _device_package + ".bit.gz";
+	bitname = resolveDataFile("bpiOverJtag",
+		"bpiOverJtag_" + _device_package + ".bit.gz");
 
 #if defined (_WIN64) || defined (_WIN32)
 	bitname = PathHelper::absolutePath(bitname);
