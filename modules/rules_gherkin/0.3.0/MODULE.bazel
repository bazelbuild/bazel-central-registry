module(
    name = "rules_gherkin",
    version = "0.3.0",
)

bazel_dep(name = "bazel_skylib", version = "1.7.1")
bazel_dep(name = "rules_cc", version = "0.2.8")

# Note: rules_gherkin no longer owns the cucumber Ruby bundle. Consumers
# declare their own `ruby.bundle_fetch(...)` and wrap it in an `rb_binary`,
# then pass that binary to each `gherkin_test(..., cucumber_ruby = ...)`.
# See README.md for the setup pattern.

bazel_dep(name = "cucumber-cpp", version = "0.8.0.bcr.1")
