load(":boost_test_suite.bzl", "boost_test_suite")

boost_test_suite(
    name = "upstream_tests",
    srcs = glob(
        ["*.cpp"],
        exclude = [
            "compile_seq_cont_rvalue_constrained_pop_back.cpp",
            "compile_sfinae_path_mutable_iterator.cpp",
            "compile_tests_main.cpp",
            # These depend on headers from the sibling example directory,
            # which is outside the standalone BCR test module.
            "static_vec.cpp",
            "static_vec_noncopyable.cpp",
            "view_adaptor.cpp",
        ],
    ),
)
