matrix:
  platform:
  - debian11
  - ubuntu2404
  - macos
  - windows
  bazel:
  - 8.x
  - 7.x
tasks:
  validate:
    name: Validate targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@cxxurl'
    test_targets:
      - '@cxxurl//:test'
