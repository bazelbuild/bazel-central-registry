---
bcr_terraform_test_module:
  module_path: "examples/terraform"
  matrix:
    platform: ["macos_arm64", "ubuntu2004", "windows"]
    bazel: ["7.x", "8.x"]
  tasks:
    run_tests:
      name: "Test Terraform"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
      test_targets:
        - "//..."

bcr_opentofu_test_module:
  module_path: "examples/opentofu"
  matrix:
    platform: ["macos_arm64", "ubuntu2004", "windows"]
    bazel: ["7.x", "8.x"]
  tasks:
    run_tests:
      name: "Test OpenTofu"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
      test_targets:
        - "//..."
