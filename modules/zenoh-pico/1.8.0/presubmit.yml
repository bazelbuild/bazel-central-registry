matrix:
  platform:
    - debian11
    - macos_arm64
    - ubuntu2204
    - ubuntu2404
  bazel: [8.x, 9.x, rolling]
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@zenoh-pico//:zenoh-pico'
bcr_test_module:
  module_path: tests
  matrix:
    linux_platform:
      - debian11
      - ubuntu2204
      - ubuntu2404
    osx_platform:
      - macos_arm64
    bazel: [8.x, 9.x, rolling]
  tasks:
    run_linux_test_module:
      name: Run linux test module
      platform: ${{ linux_platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //...
      test_targets:
        - //...
        # These work, but they take a lot of CI time, and tend to be flaky.
        - -//:z_client_test
        - -//:z_tls_verify_test
    run_osx_test_module:
      name: Run osx test module
      platform: ${{ osx_platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //...
      test_targets:
        - //...
        # These work, but they take a lot of CI time, and tend to be flaky.
        - -//:z_client_test
        - -//:z_tls_verify_test

