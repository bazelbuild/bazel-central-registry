diff --git tests/z_api_advanced_pubsub_test.c tests/z_api_advanced_pubsub_test.c
--- tests/z_api_advanced_pubsub_test.c
+++ tests/z_api_advanced_pubsub_test.c
@@ -100,6 +100,10 @@
 
         zp_config_insert(z_loan_mut(c2), Z_CONFIG_MODE_KEY, "peer");
         zp_config_insert(z_loan_mut(c2), Z_CONFIG_LISTEN_KEY, "udp/224.0.0.224:7447#iface=lo");
+    } else {
+        // Connect to the router started by api.sh without multicast discovery.
+        zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
+        zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     }
 
     ASSERT_OK(z_open(&s1, z_config_move(&c1), NULL));
@@ -678,6 +682,7 @@
     z_owned_session_t s;
     z_owned_config_t c;
     z_config_default(&c);
+    zp_config_insert(z_loan_mut(c), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k;
     ASSERT_OK(z_view_keyexpr_from_str(&k, expr));
 
diff --git tests/z_api_batching_test.c tests/z_api_batching_test.c
--- tests/z_api_batching_test.c
+++ tests/z_api_batching_test.c
@@ -24,7 +24,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
 
     ASSERT_OK(z_open(&s1, z_move(c1), NULL));
     ASSERT_OK(z_open(&s2, z_move(c2), NULL));
diff --git tests/z_api_callback_drop_on_undeclare_test.c tests/z_api_callback_drop_on_undeclare_test.c
--- tests/z_api_callback_drop_on_undeclare_test.c
+++ tests/z_api_callback_drop_on_undeclare_test.c
@@ -46,6 +46,7 @@
 z_result_t open_session(z_owned_session_t* s) {
     z_owned_config_t config;
     _Z_RETURN_IF_ERR(z_config_default(&config));
+    _Z_RETURN_IF_ERR(zp_config_insert(z_loan_mut(config), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447"));
     return z_open(s, z_config_move(&config), NULL);
 }
 
diff --git tests/z_api_cancellation_test.c tests/z_api_cancellation_test.c
--- tests/z_api_cancellation_test.c
+++ tests/z_api_cancellation_test.c
@@ -49,7 +49,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t ke;
     z_view_keyexpr_from_str(&ke, query_expr);
 
@@ -139,7 +141,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t ke;
     z_view_keyexpr_from_str(&ke, query_expr);
 
@@ -234,7 +238,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t ke;
     z_view_keyexpr_from_str(&ke, query_expr);
 
@@ -295,7 +301,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t ke;
     z_view_keyexpr_from_str(&ke, query_expr);
 
diff --git tests/z_api_liveliness_test.c tests/z_api_liveliness_test.c
--- tests/z_api_liveliness_test.c
+++ tests/z_api_liveliness_test.c
@@ -89,6 +89,10 @@
 
         zp_config_insert(z_loan_mut(c2), Z_CONFIG_MODE_KEY, "client");
         zp_config_insert(z_loan_mut(c2), Z_CONFIG_LISTEN_KEY, "udp/224.0.0.224:7447#iface=lo");
+    } else {
+        // Connect to the router started by api.sh without multicast discovery.
+        zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
+        zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     }
 
     assert_ok(z_open(&s1, z_config_move(&c1), NULL));
@@ -147,7 +151,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k, k1;
     z_view_keyexpr_from_str(&k, expr);
     z_view_keyexpr_from_str(&k1, token1_expr);
diff --git tests/z_api_matching_test.c tests/z_api_matching_test.c
--- tests/z_api_matching_test.c
+++ tests/z_api_matching_test.c
@@ -183,7 +183,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k_sub, k_pub;
     z_view_keyexpr_from_str(&k_sub, SUB_EXPR);
     z_view_keyexpr_from_str(&k_pub, PUB_EXPR);
@@ -263,7 +265,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k_queryable, k_querier, k_queryable_wild, k_queryable_wrong;
     z_view_keyexpr_from_str(&k_queryable, QUERYABLE_EXPR);
     z_view_keyexpr_from_str(&k_queryable_wild, QUERYABLE_EXPR_WILD);
@@ -398,7 +402,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k_sub, k_pub, k_sub_wrong;
     z_view_keyexpr_from_str(&k_sub, SUB_EXPR);
     z_view_keyexpr_from_str(&k_pub, PUB_EXPR);
@@ -457,7 +463,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k_pub, k_sub;
     z_view_keyexpr_from_str(&k_pub, PUB_EXPR);
     z_view_keyexpr_from_str(&k_sub, SUB_EXPR);
@@ -536,7 +544,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k_queryable, k_querier, k_queryable_wrong, k_queryable_wild;
     z_view_keyexpr_from_str(&k_queryable_wild, QUERYABLE_EXPR_WILD);
     z_view_keyexpr_from_str(&k_queryable, QUERYABLE_EXPR);
@@ -620,7 +630,9 @@
     z_owned_session_t s1, s2;
     z_owned_config_t c1, c2;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c2);
+    zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_view_keyexpr_t k_queryable, k_querier;
     z_view_keyexpr_from_str(&k_queryable, QUERYABLE_EXPR);
     z_view_keyexpr_from_str(&k_querier, QUERIER_EXPR);
diff --git tests/z_api_queryable_test.c tests/z_api_queryable_test.c
--- tests/z_api_queryable_test.c
+++ tests/z_api_queryable_test.c
@@ -28,6 +28,7 @@
     printf("Testing queryable key expressions...\n");
     z_owned_config_t config;
     z_config_default(&config);
+    zp_config_insert(z_loan_mut(config), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     zp_config_insert(z_loan_mut(config), Z_CONFIG_MODE_KEY, "client");
 
     z_owned_session_t s;
@@ -75,6 +76,7 @@
 void open_sessions(z_owned_session_t *s, z_owned_session_t *s2, bool local) {
     z_owned_config_t config;
     z_config_default(&config);
+    zp_config_insert(z_loan_mut(config), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     zp_config_insert(z_loan_mut(config), Z_CONFIG_MODE_KEY, "client");
 
     if (z_open(s, z_move(config), NULL) < 0) {
@@ -87,6 +89,7 @@
     } else {
         z_owned_config_t config2;
         z_config_default(&config2);
+        zp_config_insert(z_loan_mut(config2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
         zp_config_insert(z_loan_mut(config2), Z_CONFIG_MODE_KEY, "client");
         if (z_open(s2, z_move(config2), NULL) < 0) {
             printf("Unable to open second session for local queryable test!\n");
diff --git tests/z_api_source_info_test.c tests/z_api_source_info_test.c
--- tests/z_api_source_info_test.c
+++ tests/z_api_source_info_test.c
@@ -104,6 +104,7 @@
 void test_source_info(bool put, bool publisher, bool local_subscriber) {
     z_owned_config_t c1;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
 
     z_owned_session_t s1;
     assert_ok(z_open(&s1, z_config_move(&c1), NULL));
@@ -113,6 +114,7 @@
     if (!local_subscriber) {
         z_owned_config_t c2;
         z_config_default(&c2);
+        zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
 
         assert_ok(z_open(&s2, z_config_move(&c2), NULL));
 
@@ -253,6 +255,7 @@
 
     z_owned_config_t c1;
     z_config_default(&c1);
+    zp_config_insert(z_loan_mut(c1), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
 
     z_owned_session_t s1;
     assert_ok(z_open(&s1, z_config_move(&c1), NULL));
@@ -261,6 +264,7 @@
     if (!local_queryable) {
         z_owned_config_t c2;
         z_config_default(&c2);
+        zp_config_insert(z_loan_mut(c2), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
 
         assert_ok(z_open(&s2, z_config_move(&c2), NULL));
     }
diff --git tests/z_multi_pubsub_test.c tests/z_multi_pubsub_test.c
--- tests/z_multi_pubsub_test.c
+++ tests/z_multi_pubsub_test.c
@@ -108,7 +108,9 @@
     z_owned_session_t s_pub, s_sub;
     z_owned_config_t c_pub, c_sub;
     z_config_default(&c_pub);
+    zp_config_insert(z_loan_mut(c_pub), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c_sub);
+    zp_config_insert(z_loan_mut(c_sub), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
 
     z_view_keyexpr_t k;
     ASSERT_OK(z_view_keyexpr_from_str(&k, expr));
diff --git tests/z_multi_queryable_test.c tests/z_multi_queryable_test.c
--- tests/z_multi_queryable_test.c
+++ tests/z_multi_queryable_test.c
@@ -121,7 +121,9 @@
     z_owned_session_t s_q, s_cli;
     z_owned_config_t c_q, c_cli;
     z_config_default(&c_q);
+    zp_config_insert(z_loan_mut(c_q), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
     z_config_default(&c_cli);
+    zp_config_insert(z_loan_mut(c_cli), Z_CONFIG_CONNECT_KEY, "tcp/127.0.0.1:7447");
 
     ASSERT_OK(z_open(&s_q, z_config_move(&c_q), NULL));
     ASSERT_OK(z_open(&s_cli, z_config_move(&c_cli), NULL));
