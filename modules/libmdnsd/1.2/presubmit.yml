matrix:
  bazel: ["7.x", "8.x", "9.*"]
  platform: ["debian10", "macos", "macos_arm64", "ubuntu2004"]

tasks:
  verify_targets:
    name: Verify build targets
    bazel: ${{ bazel }}
    platform: ${{ platform }}
    build_targets:
      - "@libmdnsd"
