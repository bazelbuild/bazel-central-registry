tasks:
  verify_targets:
    name: Verify build targets
    platform: macos
    bazel: 7.x
    build_targets:
      - '@swift_argument_parser//:ArgumentParser'
    build_flags:
      - --macos_minimum_os=12.0
