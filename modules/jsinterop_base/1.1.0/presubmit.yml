matrix:
  platform:
    - macos
    - ubuntu2004
  bazel:
    - 8.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@jsinterop_base//java/..."
