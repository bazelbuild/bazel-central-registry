matrix:
  platform: [ubuntu2204]
  bazel: [7.x, 8.x]
tasks:
  verify_targets:
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rsyslog"
    test_targets:
      - "@rsyslog//..."
