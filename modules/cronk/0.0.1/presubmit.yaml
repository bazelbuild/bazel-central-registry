matrix:
  bazel: ["6.x", "7.x", "8.x", "9.x"]
  platform:
  - rockylinux8
  - debian10
  - ubuntu2004
  - macos
  - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@cronk//:cronk'
    - '@cronk//:cronk_test'
