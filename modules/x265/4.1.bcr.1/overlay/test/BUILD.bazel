load("@rules_cc//cc:cc_test.bzl", "cc_test")

# TestBench only compares the primitives against their C references. This one
# runs a few frames through the public encoder API, which is what exercises the
# per-platform pieces: the threading backend and the bitstream writer.
cc_test(
    name = "encode_smoke_test",
    size = "small",
    srcs = ["encode_smoke_test.cpp"],
    deps = ["//:x265"],
)
