matrix:
  platform:
  - debian10
  - ubuntu2204
  - macos
  - macos_arm64
  - windows
  bazel:
  - rolling
  - 9.x
  - 8.x
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    test_targets:
    - "@cpu_features//..."
