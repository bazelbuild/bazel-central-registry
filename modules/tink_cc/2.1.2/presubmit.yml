matrix:
  platform:
  - ubuntu2004
  - macos
  - windows
  bazel: [6.x]
tasks:
  verify_targets:
    name: Verify tink-cc build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
    - '@tink-cc//tink:tink_cc'
