load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "version_test",
    srcs = ["version_test.cc"],
    data = [
        "//:MODULE.bazel",
        "//:dnsmasq",
    ],
    env = {
        "DNSMASQ_RLOCATIONPATH": "$(rlocationpath //:dnsmasq)",
        "MODULE_BAZEL_RLOCATIONPATH": "$(rlocationpath //:MODULE.bazel)",
    },
    deps = ["@rules_cc//cc/runfiles"],
)
