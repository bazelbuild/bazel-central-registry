bcr_test_module:
  module_path: ""
  matrix:
    platform:
      - macos_arm64
      - ubuntu2404
      - ubuntu2404_arm64
    bazel:
      - 9.x
  tasks:
    run_tests:
      name: "Build everything"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "//..."
