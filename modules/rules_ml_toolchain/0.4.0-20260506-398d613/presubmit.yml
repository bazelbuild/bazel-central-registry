matrix:
  platform: ["ubuntu2404", "ubuntu2404_arm64"]
  bazel:
  - 7.x
  - 8.x
  - 9.x
  - rolling
tasks:
  run_tests:
    name: "Run test targets"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    test_targets:
      - "@rules_ml_toolchain//cc/tests/cpu:all"

bcr_test_module:
  module_path: tests
  matrix:
    platform: ["ubuntu2404", "ubuntu2404_arm64"]
    bazel:
    - 7.x
    - 8.x
    - 9.x
    - rolling
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - //...
      test_targets:
        - //...
