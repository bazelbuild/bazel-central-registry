load("@python_version_repo//:py_version.bzl", "USE_PYWRAP_RULES")
load("@rules_shell//shell:sh_test.bzl", "sh_test")

sh_test(
    name = "python_version_extension_smoke",
    srcs = ["python_version_extension_smoke.sh"],
    args = [str(USE_PYWRAP_RULES)],
)
