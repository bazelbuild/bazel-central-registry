bcr_test_module:
  module_path: ""
  matrix:
    platform: ["ubuntu2404", "macos_arm64", "windows"]
    bazel:
    - 7.x
    - 8.x
  tasks:
    verify_targets:
      name: "Run tests"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//cc/tests/cpu:all"
