diff --git a/tests/open_datafile.c b/tests/open_datafile.c
index 71ca91a..fb49df7 100644
--- a/tests/open_datafile.c
+++ b/tests/open_datafile.c
@@ -25,6 +25,10 @@ along with this program. If not, see http://www.gnu.org/licenses/ .
 #include <string.h>
 #include "mpc-tests.h"
 
+#ifdef MPC_BAZEL_TEST
+#include "bazel/runfiles_util.h"
+#endif
+
 void
 open_datafile (mpc_datafile_context_t* datafile_context,
                const char * data_filename)
@@ -36,6 +40,31 @@ open_datafile (mpc_datafile_context_t* datafile_context,
   if (src_dir == NULL)
     src_dir = default_srcdir;
 
+#ifdef MPC_BAZEL_TEST
+  {
+    const char *srcdir_file = getenv ("MPC_SRCDIR");
+    if (srcdir_file != NULL)
+      {
+        char *resolved = mpc_rlocation (srcdir_file);
+        if (resolved != NULL)
+          {
+            const char *sep = strrchr (resolved, '/');
+            if (sep == NULL)
+              sep = strrchr (resolved, '\\');
+            if (sep != NULL)
+              {
+                size_t dirlen = (size_t)(sep - resolved);
+                char *dir = (char *) malloc (dirlen + 1);
+                memcpy (dir, resolved, dirlen);
+                dir[dirlen] = '\0';
+                src_dir = dir;
+              }
+            free (resolved);
+          }
+      }
+  }
+#endif
+
   datafile_context->pathname =
     (char *) malloc ((strlen (src_dir)) + strlen (data_filename) + 2);
   if (datafile_context->pathname == NULL)
diff --git a/tests/read_data.c b/tests/read_data.c
index 6a7188d..83fdf7a 100644
--- a/tests/read_data.c
+++ b/tests/read_data.c
@@ -22,6 +22,10 @@ along with this program. If not, see http://www.gnu.org/licenses/ .
 #include <string.h>
 #include "mpc-tests.h"
 
+#ifdef MPC_BAZEL_TEST
+#include "bazel/runfiles_util.h"
+#endif
+
 char *pathname;
 unsigned long line_number;
    /* file name with complete path and currently read line;
@@ -52,6 +56,31 @@ open_data_file (const char *file_name)
   if (src_dir == NULL)
     src_dir = default_srcdir;
 
+#ifdef MPC_BAZEL_TEST
+  {
+    const char *srcdir_file = getenv ("MPC_SRCDIR");
+    if (srcdir_file != NULL)
+      {
+        char *resolved = mpc_rlocation (srcdir_file);
+        if (resolved != NULL)
+          {
+            const char *sep = strrchr (resolved, '/');
+            if (sep == NULL)
+              sep = strrchr (resolved, '\\');
+            if (sep != NULL)
+              {
+                size_t dirlen = (size_t)(sep - resolved);
+                char *dir = (char *) malloc (dirlen + 1);
+                memcpy (dir, resolved, dirlen);
+                dir[dirlen] = '\0';
+                src_dir = dir;
+              }
+            free (resolved);
+          }
+      }
+  }
+#endif
+
   pathname = (char *) malloc ((strlen (src_dir)) + strlen (file_name) + 2);
   if (pathname == NULL)
     {
