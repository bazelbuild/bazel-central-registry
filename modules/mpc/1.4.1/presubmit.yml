matrix:
  platform:
    - ubuntu2204
  bazel:
    - 8.x
    - 9.x
tasks:
  verify_targets:
    name: Verify build and test targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@mpc//:mpc"
      - "@mpc//tests:mpc_test"
    test_targets:
      - "@mpc//tests:mpc_test"
