===================================================================
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -5,9 +5,11 @@
 # overrides (e.g. https://github.com/bazel-contrib/rules_go/issues/4380).
 # The publish.yaml GitHub Action patches in the release version when
 # submitting to the Bazel Central Registry.
 #
-# This note must stay *outside* the `module()` call. A commented-out
+# This note must stay *outside* the `module(,
+    version = "0.0.1",
+)` call. A commented-out
 # `version = ""` inside it is what the patch generator matches, so it rewrote
 # this comment instead of adding a real attribute: the 0.2.0 registry entry
 # declared no version at all, and BCR's presubmit died during pipeline
 # generation — 26 seconds, zero jobs, zero annotations. See
