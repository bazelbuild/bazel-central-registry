===================================================================
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -21,8 +21,9 @@
 # commit* that introduces the incompatible (breaking) change.
 
 module(
     name = "rules_flutter",
+    version = "0.0.1",
 )
 
 bazel_dep(name = "bazel_skylib", version = "1.9.2")
 bazel_dep(name = "package_metadata", version = "0.0.13")
