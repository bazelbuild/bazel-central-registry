load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "cairo_missing",
    srcs = [
        "getline.c",
        "strndup.c",
    ],
    hdrs = ["cairo-missing.h"],
    copts = [
        "-DCAIRO_COMPILATION",
        "-D_GNU_SOURCE",
        "-pthread",
    ],
    includes = ["."],
    visibility = ["//visibility:public"],
    deps = [
        "//:config",
        "//src:cairo",
    ],
)
